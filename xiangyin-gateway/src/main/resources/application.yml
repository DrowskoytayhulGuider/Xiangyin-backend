server:
  port: 9999

spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      username: @nacos.username@
      password: @nacos.password@
      discovery:
        server-addr: ${NACOS_HOST:xiangyin-register}:${NACOS_PORT:8848}
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
    gateway:
      server:
        webflux:
          routes:
            - id: openapi
              uri: lb://xiangyin-gateway
              predicates:
                - Path=/v3/api-docs/**
              filters:
                - RewritePath=/v3/api-docs/(?<path>.*), /$\{path}/$\{path}/v3/api-docs
            - id: xiangyin-auth
              uri: lb://xiangyin-auth
              predicates:
                - Path=/auth/**
            - id: xiangyin-upms-biz
              uri: lb://xiangyin-upms-biz
              predicates:
                - Path=/admin/**
              metadata:
                response-timeout: 30000
            - id: xiangyin-codegen
              uri: lb://xiangyin-codegen
              predicates:
                - Path=/gen/**
            - id: xiangyin-quartz
              uri: lb://xiangyin-quartz
              predicates:
                - Path=/job/**
            - id: xiangyin-monitor
              uri: lb://xiangyin-monitor
              predicates:
                - Path=/monitor/**
  config:
    import:
      - optional:nacos:application-@profiles.active@.yml
      - optional:nacos:${spring.application.name}-@profiles.active@.yml
