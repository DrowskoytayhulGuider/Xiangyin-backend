<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.sleepywang</groupId>
        <artifactId>xiangyin-dialect</artifactId>
        <version>4.1.0</version>
    </parent>

    <artifactId>xiangyin-dialect-biz</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <!-- 注册/配置中心（必须） -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>
        <!-- 自己的 api -->
        <dependency>
            <groupId>com.sleepywang</groupId>
            <artifactId>xiangyin-dialect-api</artifactId>
        </dependency>
        <!-- 数据层（用数据库才加） -->
        <dependency>
            <groupId>com.sleepywang</groupId>
            <artifactId>xiangyin-common-data</artifactId>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>druid-spring-boot-4-starter</artifactId>
        </dependency>
        <!-- 安全（鉴权） -->
        <dependency>
            <groupId>com.sleepywang</groupId>
            <artifactId>xiangyin-common-security</artifactId>
        </dependency>
        <!-- swagger -->
        <dependency>
            <groupId>com.sleepywang</groupId>
            <artifactId>xiangyin-common-swagger</artifactId>
        </dependency>
        <!-- web（必须，见下） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>com.sleepywang</groupId>
            <artifactId>xiangyin-common-datasource</artifactId>
        </dependency>
    </dependencies>
</project>